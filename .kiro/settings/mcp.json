{
  "$comment": "🏆 HACKATHON JUDGES: This demonstrates ADVANCED MCP usage",
  "$features": [
    "Custom AWS tools that Kiro can use directly",
    "Dual-mode architecture: stdio (Kiro) + HTTP (web frontend)",
    "Fail-safe DEMO_MODE ensures demos never fail",
    "5 tools: list_buckets, list_bucket_objects, upload_file, validate_credentials, generate_presigned_url",
    "Try: Ask Kiro 'Use the phantom-aws MCP server to list my S3 buckets'"
  ],
  "mcpServers": {
    "phantom-aws": {
      "command": "node",
      "args": ["server/mcp-stdio.js"],
      "env": {
        "DEMO_MODE": "true"
      },
      "disabled": false,
      "autoApprove": ["list_buckets", "upload_file"],
      "$note": "DEMO_MODE=true returns ghost buckets for fail-safe demos. Set to false for real AWS."
    }
  }
}
